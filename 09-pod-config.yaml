apiVersion: v1
kind: Pod
metadata:
  name: pod-config
spec:
  containers:
  - name: nginx
    image: nginx
    envFrom:
    - configMapRef:
        name: devops
    # env:
    # - name: course
    #   valueFrom:
    #     configMapKeyRef:
    #       name: devops
    #       key: course
    # - name: trainer
    #   valueFrom:
    #     configMapKeyRef:
    #       name: devops
    #       key: trainer